#!/bin/sh

pip install numpy --use-mirrors
pip install astropy --use-mirrors
pip install scipy --use-mirrors
pip install skimage --use-mirrors
pip install IPython --use-mirrors
pip install pygments --use-mirrors
pip install pyzmq --use-mirrors
pip install mock --use-mirrors
pip install pyavm --user-mirrors
pip install matplotlib --use-mirrors
